# URL Shortener App (MERN Stack)

## How to Run

### Backend
1. `cd backend`
2. `npm install`
3. Create `.env` file:
```
MONGO_URI=your_mongodb_connection_string
BASE_URL=http://localhost:5000
PORT=5000
```
4. `npm run dev`

### Frontend
1. `cd frontend`
2. `npm install`
3. `npm run dev`
